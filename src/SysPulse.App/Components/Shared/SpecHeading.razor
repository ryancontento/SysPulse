@* A sub-heading inside a SpecCard, for cards that list several items (e.g. multiple GPUs). *@
<div class="heading" title="@Text">@Text</div>

@code {
    [Parameter, EditorRequired] public string Text { get; set; } = "";
}

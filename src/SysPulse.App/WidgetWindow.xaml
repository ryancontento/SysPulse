<Window x:Class="SysPulse.App.WidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SysPulse widget"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        UseLayoutRounding="True"
        FontFamily="{StaticResource PlexMono}"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Loaded="Window_Loaded">

    <!-- A 1U rack panel: screws on the ears, a badge, channel readouts with LED bars, and status lamps. -->
    <Window.Resources>
        <SolidColorBrush x:Key="WidgetAccent" Color="#F0A33A" />
        <SolidColorBrush x:Key="PanelText" Color="#ECE5D5" />
        <SolidColorBrush x:Key="PanelMuted" Color="#A39A88" />
        <SolidColorBrush x:Key="PanelFaint" Color="#6B6457" />
        <SolidColorBrush x:Key="PanelDivider" Color="#2E2B25" />
        <SolidColorBrush x:Key="LampOff" Color="#2A2721" />

        <Style x:Key="ChannelLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="9" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource PanelFaint}" />
        </Style>

        <Style x:Key="Readout" TargetType="TextBlock">
            <Setter Property="Width" Value="38" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="TextAlignment" Value="Right" />
            <Setter Property="Foreground" Value="{DynamicResource WidgetAccent}" />
        </Style>

        <Style x:Key="Unit" TargetType="TextBlock">
            <Setter Property="Margin" Value="1,0,0,3" />
            <Setter Property="VerticalAlignment" Value="Bottom" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="Foreground" Value="{StaticResource PanelMuted}" />
        </Style>

        <Style x:Key="Secondary" TargetType="TextBlock">
            <Setter Property="Margin" Value="8,0,0,3" />
            <Setter Property="VerticalAlignment" Value="Bottom" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Foreground" Value="{StaticResource PanelText}" />
        </Style>

        <Style x:Key="Divider" TargetType="Rectangle">
            <Setter Property="Width" Value="1" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Fill" Value="{StaticResource PanelDivider}" />
        </Style>

        <Style x:Key="Screw" TargetType="Ellipse">
            <Setter Property="Width" Value="7" />
            <Setter Property="Height" Value="7" />
            <Setter Property="Stroke" Value="#0A0908" />
            <Setter Property="StrokeThickness" Value="1" />
            <Setter Property="Fill">
                <Setter.Value>
                    <RadialGradientBrush GradientOrigin="0.35,0.35">
                        <GradientStop Color="#5A544A" Offset="0" />
                        <GradientStop Color="#2A2721" Offset="1" />
                    </RadialGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatusLamp" TargetType="Ellipse">
            <Setter Property="Width" Value="6" />
            <Setter Property="Height" Value="6" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Fill" Value="{StaticResource LampOff}" />
        </Style>

        <Style x:Key="HideButton" TargetType="Button">
            <Setter Property="Width" Value="22" />
            <Setter Property="Height" Value="22" />
            <Setter Property="Foreground" Value="{StaticResource PanelFaint}" />
            <Setter Property="FontFamily" Value="Segoe Fluent Icons, Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome" Background="Transparent" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#27251F" />
                                <Setter Property="Foreground" Value="{StaticResource PanelText}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="10" CornerRadius="5" BorderBrush="#3B372F" BorderThickness="1" Cursor="SizeAll">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#25231E" Offset="0" />
                <GradientStop Color="#141310" Offset="1" />
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.55" />
        </Border.Effect>
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Open SysPulse" Click="Open_Click" />
                <MenuItem x:Name="TopmostItem" Header="Always on top" IsCheckable="True" IsChecked="True" Click="Topmost_Click" />
                <Separator />
                <MenuItem Header="Hide widget" Click="Hide_Click" />
            </ContextMenu>
        </Border.ContextMenu>

        <StackPanel Orientation="Horizontal" Height="56">
            <Ellipse Style="{StaticResource Screw}" Margin="10,0" />

            <!-- Badge -->
            <StackPanel VerticalAlignment="Center" Margin="0,0,14,0">
                <StackPanel Orientation="Horizontal">
                    <Border x:Name="PowerLamp" Width="6" Height="6" CornerRadius="3" Margin="0,1,7,0"
                            VerticalAlignment="Center" Background="{DynamicResource WidgetAccent}">
                        <Border.Effect>
                            <DropShadowEffect x:Name="PowerLampGlow" BlurRadius="8" ShadowDepth="0" Opacity="0.9" Color="#F0A33A" />
                        </Border.Effect>
                    </Border>
                    <TextBlock FontSize="12">
                        <Run FontWeight="SemiBold" Foreground="{StaticResource PanelText}" Text="SYS" /><Run Foreground="{StaticResource PanelMuted}" Text="PULSE" />
                    </TextBlock>
                </StackPanel>
                <TextBlock x:Name="ProfileText" Margin="13,3,0,0" FontSize="9" Foreground="{StaticResource PanelFaint}" Text="PROFILE" ToolTip="Current profile" />
            </StackPanel>

            <Rectangle Style="{StaticResource Divider}" />

            <!-- CPU -->
            <StackPanel VerticalAlignment="Center" Margin="12,0">
                <TextBlock Style="{StaticResource ChannelLabel}" Text="CPU" />
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="CpuLoad" Style="{StaticResource Readout}" Text="--" />
                    <TextBlock Style="{StaticResource Unit}" Text="%" />
                    <TextBlock x:Name="CpuTemp" Style="{StaticResource Secondary}" Width="44" Text="--" />
                </StackPanel>
                <UniformGrid x:Name="CpuBar" Rows="1" Height="3" Margin="0,3,0,0" />
            </StackPanel>

            <Rectangle Style="{StaticResource Divider}" />

            <!-- GPU -->
            <StackPanel VerticalAlignment="Center" Margin="12,0">
                <TextBlock Style="{StaticResource ChannelLabel}" Text="GPU" />
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="GpuLoad" Style="{StaticResource Readout}" Text="--" />
                    <TextBlock Style="{StaticResource Unit}" Text="%" />
                    <TextBlock x:Name="GpuTemp" Style="{StaticResource Secondary}" Width="44" Text="--" />
                </StackPanel>
                <UniformGrid x:Name="GpuBar" Rows="1" Height="3" Margin="0,3,0,0" />
            </StackPanel>

            <Rectangle Style="{StaticResource Divider}" />

            <!-- Memory -->
            <StackPanel VerticalAlignment="Center" Margin="12,0">
                <TextBlock Style="{StaticResource ChannelLabel}" Text="RAM" />
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="RamLoad" Style="{StaticResource Readout}" Text="--" />
                    <TextBlock Style="{StaticResource Unit}" Text="%" />
                    <TextBlock x:Name="RamUsed" Style="{StaticResource Secondary}" Width="52" Text="--" />
                </StackPanel>
                <UniformGrid x:Name="RamBar" Rows="1" Height="3" Margin="0,3,0,0" />
            </StackPanel>

            <Rectangle Style="{StaticResource Divider}" />

            <!-- Network -->
            <StackPanel VerticalAlignment="Center" Margin="12,0" Width="96">
                <TextBlock Style="{StaticResource ChannelLabel}" Text="NET" />
                <TextBlock x:Name="NetDown" Margin="0,2,0,0" FontSize="11" Foreground="{StaticResource PanelText}" Text="▼ --" />
                <TextBlock x:Name="NetUp" FontSize="11" Foreground="{StaticResource PanelMuted}" Text="▲ --" />
            </StackPanel>

            <Rectangle Style="{StaticResource Divider}" />

            <!-- Status lamps -->
            <StackPanel VerticalAlignment="Center" Margin="12,0,6,0">
                <StackPanel Orientation="Horizontal" ToolTip="Lights when the CPU or GPU is slowing itself down">
                    <Ellipse x:Name="ThrottleLamp" Style="{StaticResource StatusLamp}" />
                    <TextBlock Style="{StaticResource ChannelLabel}" Text="THROTTLE" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0" ToolTip="Lights while an alert rule is triggered">
                    <Ellipse x:Name="AlertLamp" Style="{StaticResource StatusLamp}" />
                    <TextBlock Style="{StaticResource ChannelLabel}" Text="ALERT" />
                </StackPanel>
            </StackPanel>

            <Button Style="{StaticResource HideButton}" Content="&#xE8BB;" ToolTip="Hide widget" Click="Hide_Click" VerticalAlignment="Top" Margin="0,4,0,0" />

            <Ellipse Style="{StaticResource Screw}" Margin="6,0,10,0" />
        </StackPanel>
    </Border>
</Window>
